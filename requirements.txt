# The engine itself needs nothing beyond the standard library.
# Everything below is for a storage backend or the tooling around a run.

# Parquet store (the default) — recommended
pyarrow>=14.0

# SQL store: PostgreSQL, SQLite, TimescaleDB, ...
SQLAlchemy>=2.0
psycopg2-binary>=2.9   # PostgreSQL driver; drop it if you only use SQLite

# YAML run configs (JSON works without it)
PyYAML>=6.0

# Tests
pytest>=7.4

# Reading CME's margin history PDF (scripts/margins.py import). Only needed
# when importing; everything else works without it.
pypdf>=4.0
# openpyxl>=3.1   # uncomment if CME hands you .xlsx instead of .pdf/.csv

# Ad-hoc analysis: SQL straight against the Parquet store, no import step.
# Note it renders timestamps in the session timezone — `SET TimeZone='UTC';`
# first, or the day boundary will not be where you think it is.
duckdb>=0.10

# Optional, for poking at runs/*/trades.csv
# pandas>=2.0
# matplotlib>=3.8
